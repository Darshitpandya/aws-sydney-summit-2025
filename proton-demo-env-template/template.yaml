AWSTemplateFormatVersion: '2010-09-09'
Description: Minimal Proton Environment Template (Serverless-Friendly)

Resources:
  DummyLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: !Sub /aws/proton/demo-env-${AWS::StackName}
      RetentionInDays: 1
